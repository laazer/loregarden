handoff:
  schema_version: '1.0'
  ticket_id: 164-parent-auto-mode-run-a-ticket-subtree-end-to-end
  from_agent: test_breaker
  to_agent: backend_implementer
  validated_at: '2026-07-22T23:10:15Z'
  required_items_met: 4
  total_required_items: 4
  checklist:
  - item_key: test_suite_complete
    item: Test suite covers all acceptance criteria
    required: true
    status: complete
    evidence: 'server/tests/test_auto_mode_subtree.py — 20 tests covering: auto_approve propagation (child/grandchild/off),
      gate_required auto-resolve (on/off), agentless gate auto-resolve (on/off), audit-row distinguishability
      (auto vs human resolved_by, resolving_orchestration_run_id), AskUserQuestion still pausing under
      auto_approve, BLOCKED stopping the subtree (direct + grandchild), subtree-wide bound (basic, strict,
      exact boundary, one-under boundary, zero=unlimited), already-done child not re-run, deterministic
      child ordering, and full 2-child e2e crossing both gate shapes.'
  - item_key: test_all_runnable
    item: All tests in the suite are runnable and collect cleanly
    required: true
    status: complete
    evidence: pytest server/tests/test_auto_mode_subtree.py collects and runs all 20 tests with no collection
      errors, no skips; ruff check and ruff format --check both clean.
  - item_key: red_state_confirmed
    item: Tests fail for the right reason against real pre-implementation code
    required: true
    status: complete
    evidence: '12 of 20 tests fail, all attributable to the two documented gaps: BuiltinOrchestrator.execute()
      at builtin_orchestrator.py:1005 does not pass auto_approve to child execute() calls, and Approval/OrchestrationProfile
      lack resolved_by, resolving_orchestration_run_id, and max_subtree_stages_per_run. Verified by reading
      the current source directly, not inferred from test names. Re-ran twice for determinism (no flakiness).'
  - item_key: adversarial_gaps_documented
    item: Gaps found by adversarial review are recorded for the implementer
    required: true
    status: complete
    evidence: 'loregarden_attach_artifact on this run: 4 new tests added covering (1) human-resolved approvals
      must not read as resolved_by=''automation'', (2) a ticket already AWAITING before auto_approve was
      ever set must still resolve when later swept into an auto run (design decision flagged, not just
      a bug), (3) exact-boundary correctness of max_subtree_stages_per_run, (4) 0=unlimited convention
      for the new bound field.'
