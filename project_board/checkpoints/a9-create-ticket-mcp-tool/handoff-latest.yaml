handoff:
  schema_version: '1.0'
  ticket_id: a9-create-ticket-mcp-tool
  from_agent: backend_implementer
  to_agent: architecture_reviewer
  validated_at: '2026-07-24T23:09:04Z'
  required_items_met: 4
  total_required_items: 4
  checklist:
  - item_key: implementation_complete
    item: Implementation satisfies every acceptance criterion
    required: true
    status: complete
    evidence: 'All 3 prior-review rounds resolved: (1) execute_tool() at mcp/tools.py:1226 gates loregarden_create_ticket
      via is_orchestrated_agent_denied_mcp_tool(name) on the real `orchestrated` dispatch-layer flag (threaded
      from the X-Loregarden-Orchestrated header set only by Loregarden''s own CLI invocation builders),
      not just the CLI-subprocess permission_bridge.py side channel; (2) review agent-run failures were
      transient tooling failures, not code issues -- re-verified the diff cleanly; (3) _create_ticket
      now takes svc: OrchestrationCallbackService and resolves parent via svc.resolve_ticket(ticket_id=parent)
      (catching ValueError to raise ''Parent ticket not found: <parent>'', preserving distinct no-parent
      vs parent-not-found messaging) instead of the removed duplicate _resolve_parent_ticket_id lookup'
  - item_key: tests_pass
    item: New and existing tests pass
    required: true
    status: complete
    evidence: '90 passed: test_mcp_create_ticket.py, test_mcp_create_ticket_access_control.py, test_mcp_create_ticket_adversarial.py,
      test_mcp_all_tools_smoke.py, test_permission_bridge.py; ruff check clean on mcp/tools.py and permission_bridge.py'
  - item_key: access_control_decision_recorded
    item: Orchestrated-agent access decision implemented per triage/test-design checkpoints
    required: true
    status: complete
    evidence: 'Unchanged from prior round, now confirmed enforced at the real dispatch entrypoint: permission_bridge.py
      ORCHESTRATED_DENIED_MCP_TOOLS + is_orchestrated_agent_denied_mcp_tool(), checked in both PermissionBridgeRunner._try_fast_approve
      (CLI-subprocess path) and execute_tool() itself (direct HTTP / EXTERNAL_MCP path) via the orchestrated
      flag threaded from the X-Loregarden-Orchestrated header'
  - item_key: debt_recorded
    item: Interim allowlist recorded as debt pending a2-per-agent-server-policy
    required: true
    status: complete
    evidence: execute_tool() docstring in mcp/tools.py explicitly documents the orchestrated flag's threading
      path, its current coverage (HTTP header only), and cites a9-create-ticket-mcp-tool as the ticket
      recording this as debt pending a2-per-agent-server-policy's real per-agent x per-server policy table
