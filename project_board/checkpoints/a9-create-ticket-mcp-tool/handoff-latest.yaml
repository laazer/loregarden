handoff:
  schema_version: '1.0'
  ticket_id: a9-create-ticket-mcp-tool
  from_agent: test_designer
  to_agent: test_breaker
  validated_at: '2026-07-24T17:04:18Z'
  required_items_met: 3
  total_required_items: 3
  checklist:
  - item_key: tests_cover_acceptance_criteria
    item: Tests cover every acceptance criterion
    required: true
    status: complete
    evidence: server/tests/test_mcp_create_ticket.py (registration, happy path, validation delegation,
      parent UUID/slug resolution, HTTP layer), test_mcp_create_ticket_adversarial.py, and test_mcp_create_ticket_access_control.py
      + new tests in test_permission_bridge.py covering the orchestrated-agent access-control AC end to
      end
  - item_key: tests_deterministic
    item: Tests are deterministic and behavioral, not interaction-based
    required: true
    status: complete
    evidence: All new tests assert on persisted Ticket rows, tool return payloads, and real control-response
      bytes written to a fake CLI stdin — no internal-call mocking beyond the external CLI process boundary
  - item_key: spec_gaps_reported
    item: Spec gaps or ambiguities found during test design are reported
    required: true
    status: complete
    evidence: 'loregarden_append_checkpoint: found the orchestrated-agent deny check in permission_bridge.py._try_fast_approve
      is unconditional (not scoped by track_workflow_stage), so it wrongly denies loregarden_create_ticket
      to interactive Ticket Studio chat too, contradicting the ticket''s own AC and the prior implement-stage
      handoff''s claim; two new tests in test_permission_bridge.py pin this (one green confirming orchestrated
      denial is correct, one red exposing the interactive-context bug) for the implement stage to fix'
