{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Least-privilege capability for the main window — dialogs, clipboard, notifications, and opening external links only. No filesystem or shell-execute access is exposed to the webview; the backend sidecar is spawned exclusively from trusted Rust setup code, which this capability file does not gate.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-save",
    "clipboard-manager:allow-read-text",
    "clipboard-manager:allow-write-text",
    "notification:allow-is-permission-granted",
    "notification:allow-request-permission",
    "notification:allow-notify",
    "opener:allow-default-urls"
  ]
}
