# Loregarden
/data/
/data/loregarden.db
/data/loregarden.db-*
*.db
*.db-journal
*.db-wal
*.db-shm
server/.venv/
server/.self-improve-restart
server/.pyinstaller/
server/.pylint_home/
.loregarden/
client/node_modules/
client/dist/
node_modules/

# Secrets
.env
.envrc

# Python
__pycache__/
*.py[cod]
*.pyc
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# OS / editor
.DS_Store

# Visual QA output (screenshots + summary), regenerated per run
client/.visual-qa/

# Design explorations — kept locally, not part of the repo
docs/design/

# Local agent state — was ignored only by ~/.git/.gitignore or .git/info/exclude,
# neither of which travels with a clone. Kept here so a fresh checkout is safe.
.claude/settings.local.json
.claude/worktrees/
scratch/
tmp/
temp/
draft/

# Tool caches
.ruff_cache/
.uv-cache/
.venv/

# Stray test/run output
*.log
